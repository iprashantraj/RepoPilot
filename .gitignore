# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.venv/
venv/
.python-version.local

# uv: keep uv.lock committed for reproducible installs.

# Local LLM cache
.cache/

# Env
.env
# Anchored to the repo root so it covers .bak/.old copies made while migrating
# hosts, without matching the committed infra/deploy/.env.deploy.example.
/.env.deploy*
.env.local
.env.*.local

# Node
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Eval reports (timestamped JSON+Markdown produced by `python -m repopilot_evals run --report`)
eval-reports/

# Playwright output
test-results/
playwright-report/

# Local scratch: one-off scripts and timing notes, kept out of the tree that
# everyone else clones.
scratch_*.py
*_index_time.txt
